<%= turbo_stream.replace "messages" do %>
  <%= turbo_frame_tag "messages" do %>
  <%= render "messages/messages", messages: @messages, other_user: @other_user, chat: @chat, message: @message, viewer: current_user %>
  <% end %>
<% end %>
